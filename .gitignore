node_modules/
.wrangler/
.dev.vars

scripts/seed-uis-catalog.sql

.env

.mcpregistry_github_token
.mcpregistry_registry_token

# credenciais que caem no repo por engano (caminho mal escapado em comando de
# shell gera nomes como "UsersSIDNEY.snyktoken.txt" na raiz — já aconteceu)
*token*.txt
*.token
snyk.json

# Roadmap tecnico: planejamento interno, nao vai para repo publico. Mesma
# convencao dos irmaos — ilo-mcp-server e ibge-br-mcp (.gitignore: ROADMAP.md),
# medical-terminologies-mcp (PROGRESS.md). O painel do portfolio-monitor le o
# arquivo LOCAL e renderiza o HTML fora do repositorio do produto.
ROADMAP.md
